# Benchmark Artifacts

This directory is intentionally empty at bootstrap. Future benchmark runs will store reproducible artifacts here according to [the benchmark methodology](../docs/BENCHMARKS.md).

No benchmark has been run and no result is claimed.
