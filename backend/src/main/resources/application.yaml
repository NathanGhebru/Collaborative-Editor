spring:
  application:
    name: collaborative-editor-backend

server:
  port: ${BACKEND_PORT:8080}

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
